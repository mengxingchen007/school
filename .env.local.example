# 复制这个文件为 .env.local（本地）或直接在 Vercel 的 Environment Variables 里填写
# 这两个值在 Supabase 项目里：Settings -> API
# 只需要 Project URL 和 anon public key，不要用 service_role（那个是密钥，不能公开）

NEXT_PUBLIC_SUPABASE_URL=你的项目URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的anon public key
